[project]
name = "sales-project"
version = "0.1.0"
description = "A beginner dbt project using DuckDB"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "dbt-core>=1.10,<2.0",
    "dbt-duckdb>=1.10,<2.0",
]

[tool.uv]
package = false
