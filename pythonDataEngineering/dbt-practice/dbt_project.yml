name: sales_project
version: "1.0.0"
config-version: 2

profile: sales_project

model-paths: ["models"]
seed-paths: ["seeds"]
test-paths: ["tests"]
macro-paths: ["macros"]
snapshot-paths: ["snapshots"]

target-path: "target"
clean-targets:
  - "target"
  - "dbt_packages"

models:
  sales_project:
    staging:
      +materialized: view
    marts:
      +materialized: table
